<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Workout</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav me-auto mb-2 mb-md-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/athletes_den">Athletes Den</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/workout_lounge">Workout Lounge</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/your_workout">Show off your workout</a>
          </li>
        </ul>
        <div class="d-flex gap-1">
          <% if current_user.blank? %>
            <a class="btn btn-outline-info" href="<%= new_user_registration_path %>">Sign up</a>
            <a class="btn btn-success" href="<%= new_user_session_path %>">Log in</a>
          <% else %>
            <%= link_to "Sign out", destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: 'btn btn-outline-danger' %>
          <% end %>
        </div>
      </div>
    </div>
  </nav>
</header>